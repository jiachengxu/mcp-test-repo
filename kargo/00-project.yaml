apiVersion: kargo.akuity.io/v1alpha1
kind: Project
metadata:
  name: nginx
spec:
  promotionPolicies:
    - stage: nginx-test
      autoPromotionEnabled: true
    - stage: nginx-staging
      autoPromotionEnabled: true
    - stage: nginx-prod
      autoPromotionEnabled: true
