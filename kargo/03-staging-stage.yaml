apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: nginx-staging
  namespace: nginx
  annotations:
    kargo.akuity.io/argocd-context: '[{"name":"nginx-staging","namespace":"argocd"}]'
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: nginx
      sources:
        stages:
          - nginx-test
  promotionTemplate:
    spec:
      steps:
        - uses: git-clone
          config:
            repoURL: https://github.com/jiachengxu/mcp-test-repo
            checkout:
              - branch: main
                path: ./repo
        - uses: kustomize-set-image
          as: update-image
          config:
            path: ./repo/apps/nginx/overlays/staging
            images:
              - image: mirror.gcr.io/library/nginx
                tag: '{{ imageFrom("mirror.gcr.io/library/nginx").Tag }}'
        - uses: git-commit
          as: commit
          config:
            path: ./repo
            message: 'chore(staging): promote {{ imageFrom("mirror.gcr.io/library/nginx").Tag }}'
        - uses: git-push
          config:
            path: ./repo
        - uses: argocd-update
          config:
            apps:
              - name: nginx-staging
                namespace: argocd
                sources:
                  - repoURL: https://github.com/jiachengxu/mcp-test-repo
                    desiredRevision: '{{ task.outputs.commit.commit }}'
