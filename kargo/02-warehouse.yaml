apiVersion: kargo.akuity.io/v1alpha1
kind: Warehouse
metadata:
  name: nginx
  namespace: nginx
spec:
  subscriptions:
    - image:
        repoURL: polinux/stress
        imageSelectionStrategy: Digest
        allowTags: ^latest$
        discoveryLimit: 1
